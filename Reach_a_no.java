public class Reach_a_no {
    public static void main(String[] args) {
        int target = 2;
        int ans = reachNumber(target);
        System.out.println(ans);
    }

    static int reachNumber(int target) {
        target = Math.abs(target);
        int start = 1;
        int end = target;
        int count =0;
        while (start<=end){
            int mid = start+(end-start);

        }
        return -1;
    }

}
